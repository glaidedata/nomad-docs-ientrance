[project]
name = "glaide-tutorial-ak-feldhoff"
version = "0.1.0"
description = "MkDocs site for the Glaide tutorial."
requires-python = ">=3.10"
dependencies = [
  "mkdocs",
  "mkdocs-material==8.1.1",
  "pymdown-extensions",
  "mkdocs-click",
  "mkdocstrings-python",
]

[tool.uv]
package = false
